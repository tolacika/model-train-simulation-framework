import { SimulationConfig } from "./types";

export const defaultSimulationConfig: SimulationConfig = {
    backgroundColor: '#1d293d', // Default background color
    trackBaseColor: '#ffffff', // Default track color
}